# Claude Code Project Memory

Use the root `CLAUDE.md` and the `requirements/` folder as the source of truth.

Build a premium mobile-first royal wedding invitation website with:

- full-screen intro video,
- seamless final-frame transition,
- expanding invitation card on scroll,
- subtle gold foil shine,
- paper layer reveals,
- premium shadow depth,
- English/Hindi language toggle,
- JSON-driven content.

Main data file:

```text
requirements/data/invitation-data.json
```

Use the Cloudinary video and final-frame image configured in:

```text
theme.assets.introVideo
theme.assets.finalFrame
```

Main date/time:

```text
9th May 2026, 6:30 PM
```

Countdown style:

```text
royal ivory/gold time-jewel tiles
soft 3D bevel
inner shadow
subtle gold shine
no digital clock look
```

Avoid bubbles, liquid effects, heavy petals, heavy particles, neon colors, and fast animation.
